# SubCMLs

Sub CMLs are CatalystML jsons that are intended to be run within another CML json with the runCML operation.  The usefulness of this is that it allows you to recycle commonly used transformations.

This directory contains sample subCMLs.
